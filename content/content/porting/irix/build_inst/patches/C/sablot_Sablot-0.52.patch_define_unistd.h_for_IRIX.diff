--- /projects/sise/openoffice/devel/workpits/OOo641C/workarea/sablot/Sablot-0.52.patch	Mon Jan 21 09:13:45 2002
+++ sablot/Sablot-0.52.patch	Mon Jan 21 09:13:15 2002
@@ -85,7 +85,7 @@
 ! CFLAGS+=     -DHAVE_IEEEFP_H
 ! .ENDIF
 ! 
-! .IF "$(OS)"=="LINUX" || "$(OS)"=="MACOSX"
+! .IF "$(OS)"=="LINUX" || "$(OS)"=="MACOSX" || "$(OS)"=="IRIX"
 ! CFLAGS+=      -DHAVE_UNISTD_H
 ! .ENDIF
 ! 
